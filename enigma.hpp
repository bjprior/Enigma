/*#include<fstream>

using namespace std;

// a function to check the legitimacy of the configuration inputs 

int input_check(int number_of_files, char* file_name[]);

// a function to check the plugboard file is valid 

//int plugboard_check(char* file_name);
  
// a function to check the reflector file is valid 

int reflector_check(char* file_name);

// a function to check the rotor file is valid 

int rotor_check(char* file_name);

// a funciton to check the rotor configuration file is valid 

int config_check(char* file_name, int number_of_files);

// opens a stream and checks for an error 

int open_file(char* file_name,ifstream &instream);

// checks to see if an input is numeric 

bool is_numeric(int test_digit);

// checks to see if the number is in the specified range 

bool index_check(int number);

*/
